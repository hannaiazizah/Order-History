<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/img_order_item"
        android:layout_width="96dp"
        android:layout_height="96dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:background="@color/gray_700"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/txt_order_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@color/gray_900"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/img_order_item"
        app:layout_constraintTop_toTopOf="@id/img_order_item"
        tools:text="GOOD DEAL ALPUKAT MENTEGA SEDANG 500GR" />

    <TextView
        android:id="@+id/txt_order_item_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/red"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="@id/txt_order_item_name"
        app:layout_constraintTop_toBottomOf="@id/txt_order_item_name"
        tools:text="Rp. 11.903" />

    <TextView
        android:id="@+id/txt_order_item_qty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:textColor="@color/gray_700"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@id/txt_order_item_price"
        app:layout_constraintTop_toBottomOf="@id/txt_order_item_price"
        tools:text="Jumlah: 1" />

    <TextView
        android:id="@+id/txt_order_item_status_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="4dp"
        android:textSize="10sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:background="@drawable/bg_rounded_orange"
        tools:ignore="SmallSp"
        tools:text="Pesanan dibatalkan"
        tools:textColor="@color/orange" />

</androidx.constraintlayout.widget.ConstraintLayout>